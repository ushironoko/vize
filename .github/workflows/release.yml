name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write

env:
  CARGO_INCREMENTAL: 0
  NPM_TAG: latest

jobs:
  # Build CLI binaries for all platforms
  build-cli:
    name: Build CLI - ${{ matrix.settings.target }}
    runs-on: ${{ matrix.settings.host }}
    strategy:
      fail-fast: false
      matrix:
        settings:
          - host: macos-latest
            target: x86_64-apple-darwin
            archive: vize-x86_64-apple-darwin.tar.gz
          - host: macos-latest
            target: aarch64-apple-darwin
            archive: vize-aarch64-apple-darwin.tar.gz
          - host: windows-latest
            target: x86_64-pc-windows-msvc
            archive: vize-x86_64-pc-windows-msvc.zip
          - host: windows-latest
            target: aarch64-pc-windows-msvc
            archive: vize-aarch64-pc-windows-msvc.zip
          - host: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            archive: vize-x86_64-unknown-linux-gnu.tar.gz
          - host: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            archive: vize-aarch64-unknown-linux-gnu.tar.gz
    steps:
      - uses: actions/checkout@v4

      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.settings.target }}

      - name: Install cross-compilation tools (Linux ARM64)
        if: matrix.settings.target == 'aarch64-unknown-linux-gnu'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Build CLI
        shell: bash
        env:
          CARGO_TARGET_AARCH64_UNKNOWN_LINUX_GNU_LINKER: aarch64-linux-gnu-gcc
        run: cargo build --release -p vize --target ${{ matrix.settings.target }}

      - name: Create archive (Unix)
        if: runner.os != 'Windows'
        run: |
          cd target/${{ matrix.settings.target }}/release
          tar -czvf ../../../${{ matrix.settings.archive }} vize

      - name: Create archive (Windows)
        if: runner.os == 'Windows'
        run: |
          cd target/${{ matrix.settings.target }}/release
          7z a ../../../${{ matrix.settings.archive }} vize.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: cli-${{ matrix.settings.target }}
          path: ${{ matrix.settings.archive }}
          if-no-files-found: error

  # Build native bindings (vize-native + fresco-native) for all platforms
  build-native-all:
    name: Build Native - ${{ matrix.settings.target }}
    runs-on: ${{ matrix.settings.host }}
    strategy:
      fail-fast: false
      matrix:
        settings:
          - host: macos-latest
            target: x86_64-apple-darwin
            build_vize: pnpm -C npm/vize-native build --target x86_64-apple-darwin
            build_fresco: pnpm -C npm/fresco-native build --target x86_64-apple-darwin
          - host: macos-latest
            target: aarch64-apple-darwin
            build_vize: pnpm -C npm/vize-native build --target aarch64-apple-darwin
            build_fresco: pnpm -C npm/fresco-native build --target aarch64-apple-darwin
          - host: windows-latest
            target: x86_64-pc-windows-msvc
            build_vize: pnpm -C npm/vize-native build --target x86_64-pc-windows-msvc
            build_fresco: pnpm -C npm/fresco-native build --target x86_64-pc-windows-msvc
          - host: windows-latest
            target: aarch64-pc-windows-msvc
            build_vize: pnpm -C npm/vize-native build --target aarch64-pc-windows-msvc
            build_fresco: pnpm -C npm/fresco-native build --target aarch64-pc-windows-msvc
          - host: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            build_vize: pnpm -C npm/vize-native build --target x86_64-unknown-linux-gnu
            build_fresco: pnpm -C npm/fresco-native build --target x86_64-unknown-linux-gnu
          - host: ubuntu-latest
            target: x86_64-unknown-linux-musl
            build_vize: pnpm -C npm/vize-native build --target x86_64-unknown-linux-musl --cross-compile
            build_fresco: pnpm -C npm/fresco-native build --target x86_64-unknown-linux-musl --cross-compile
          - host: ubuntu-latest
            target: aarch64-unknown-linux-gnu
            build_vize: pnpm -C npm/vize-native build --target aarch64-unknown-linux-gnu --cross-compile
            build_fresco: pnpm -C npm/fresco-native build --target aarch64-unknown-linux-gnu --cross-compile
          - host: ubuntu-latest
            target: aarch64-unknown-linux-musl
            build_vize: pnpm -C npm/vize-native build --target aarch64-unknown-linux-musl --cross-compile
            build_fresco: pnpm -C npm/fresco-native build --target aarch64-unknown-linux-musl --cross-compile
    steps:
      - uses: actions/checkout@v4

      - name: Enable long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --no-frozen-lockfile

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.settings.target }}

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Setup Zig
        if: contains(matrix.settings.build_vize, '--cross-compile')
        uses: goto-bus-stop/setup-zig@v2

      - name: Build vize-native
        run: ${{ matrix.settings.build_vize }}
        shell: bash

      - name: Upload vize-native artifact
        uses: actions/upload-artifact@v4
        with:
          name: bindings-${{ matrix.settings.target }}
          path: npm/vize-native/*.node
          if-no-files-found: error

      - name: Build fresco-native
        run: ${{ matrix.settings.build_fresco }}
        shell: bash

      - name: Upload fresco-native artifact
        uses: actions/upload-artifact@v4
        with:
          name: fresco-bindings-${{ matrix.settings.target }}
          path: npm/fresco-native/*.node
          if-no-files-found: error

  # Publish native packages to npm (using Trusted Publishing / OIDC)
  release-npm-native:
    name: Release @vizejs/native to npm
    runs-on: ubuntu-latest
    needs: build-native-all
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: bindings-*
          path: npm/vize-native/artifacts

      - name: Create npm platform directories
        working-directory: npm/vize-native
        run: npx napi create-npm-dirs

      - name: Move artifacts
        working-directory: npm/vize-native
        run: npx napi artifacts --output-dir artifacts

      - name: Prepare for publish
        working-directory: npm/vize-native
        run: npx napi pre-publish -t npm --no-gh-release --skip-optional-publish

      - name: List packages
        run: ls -R npm/vize-native/npm/

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish platform packages
        working-directory: npm/vize-native
        run: |
          for dir in npm/*/; do
            if [ -d "$dir" ]; then
              echo "Publishing $dir..."
              cd "$dir"
              npm publish --provenance --access public --tag $NPM_TAG || echo "Failed to publish $dir (may need initial manual publish)"
              cd ../..
            fi
          done

      - name: Publish
        working-directory: npm/vize-native
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Publish fresco native packages to npm (using Trusted Publishing / OIDC)
  release-npm-fresco-native:
    name: Release @vizejs/fresco-native to npm
    runs-on: ubuntu-latest
    needs: build-native-all
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: fresco-bindings-*
          path: npm/fresco-native/artifacts

      - name: Create npm platform directories
        working-directory: npm/fresco-native
        run: npx napi create-npm-dirs

      - name: Move artifacts
        working-directory: npm/fresco-native
        run: npx napi artifacts --output-dir artifacts

      - name: Prepare for publish
        working-directory: npm/fresco-native
        run: npx napi pre-publish -t npm --no-gh-release --skip-optional-publish

      - name: List packages
        run: ls -R npm/fresco-native/npm/

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish platform packages
        working-directory: npm/fresco-native
        run: |
          for dir in npm/*/; do
            if [ -d "$dir" ]; then
              echo "Publishing $dir..."
              cd "$dir"
              npm publish --provenance --access public --tag $NPM_TAG || echo "Failed to publish $dir (may need initial manual publish)"
              cd ../..
            fi
          done

      - name: Publish
        working-directory: npm/fresco-native
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish WASM package (using Trusted Publishing / OIDC)
  release-npm-wasm:
    name: Release @vizejs/wasm to npm
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Install wasm-bindgen-cli
        uses: taiki-e/install-action@v2
        with:
          tool: wasm-bindgen-cli@0.2.111

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Build WASM
        run: |
          cargo build --release -p vize_vitrine --no-default-features --features wasm --target wasm32-unknown-unknown
          wasm-bindgen target/wasm32-unknown-unknown/release/vize_vitrine.wasm --out-dir npm/vize-wasm --target web
          # Restore the correct package.json (wasm-bindgen overwrites it with crate name)
          git checkout npm/vize-wasm/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/wasm
        working-directory: npm/vize-wasm
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Vite plugin (using Trusted Publishing / OIDC)
  release-npm-vite-plugin:
    name: Release @vizejs/vite-plugin to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build vite-plugin
        run: pnpm -C npm/vite-plugin-vize build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/vite-plugin
        working-directory: npm/vite-plugin-vize
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish unplugin package (using Trusted Publishing / OIDC)
  release-npm-unplugin:
    name: Release @vizejs/unplugin to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build unplugin-vize
        run: pnpm -C npm/unplugin-vize build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/unplugin
        working-directory: npm/unplugin-vize
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Fresco package (using Trusted Publishing / OIDC)
  release-npm-fresco:
    name: Release @vizejs/fresco to npm
    runs-on: ubuntu-latest
    needs: release-npm-fresco-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build fresco
        run: pnpm -C npm/fresco build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/fresco
        working-directory: npm/fresco
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Musea MCP server (using Trusted Publishing / OIDC)
  release-npm-musea-mcp-server:
    name: Release @vizejs/musea-mcp-server to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build musea-mcp-server
        run: pnpm -C npm/musea-mcp-server build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/musea-mcp-server
        working-directory: npm/musea-mcp-server
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Vite plugin for Musea (using Trusted Publishing / OIDC)
  release-npm-vite-plugin-musea:
    name: Release @vizejs/vite-plugin-musea to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build vite-plugin-musea
        run: pnpm -C npm/vite-plugin-musea build

      - name: Build musea gallery
        run: pnpm -C npm/vite-plugin-musea build:gallery

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/vite-plugin-musea
        working-directory: npm/vite-plugin-musea
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Rspack plugin (using Trusted Publishing / OIDC)
  release-npm-rspack-plugin:
    name: Release @vizejs/rspack-plugin to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build rspack-vize-plugin
        run: pnpm -C npm/rspack-vize-plugin build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/rspack-plugin
        working-directory: npm/rspack-vize-plugin
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish Musea Nuxt (using Trusted Publishing / OIDC)
  release-npm-musea-nuxt:
    name: Release @vizejs/musea-nuxt to npm
    runs-on: ubuntu-latest
    needs: release-npm-native
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build musea-nuxt
        run: pnpm -C npm/musea-nuxt build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/musea-nuxt
        working-directory: npm/musea-nuxt
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Build and publish @vizejs/nuxt (using Trusted Publishing / OIDC)
  release-npm-nuxt:
    name: Release @vizejs/nuxt to npm
    runs-on: ubuntu-latest
    needs: [release-npm-vite-plugin, release-npm-vite-plugin-musea, release-npm-musea-nuxt]
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build @vizejs/nuxt
        run: pnpm -C npm/nuxt build

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Publish @vizejs/nuxt
        working-directory: npm/nuxt
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG

  # Publish crates to crates.io (using Trusted Publishing)
  release-crates:
    name: Release crates to crates.io
    runs-on: ubuntu-latest
    environment: crates-io
    steps:
      - uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: Get crates.io token via Trusted Publishing
        id: crates-io-auth
        uses: rust-lang/crates-io-auth-action@v1

      - name: Publish to crates.io
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.crates-io-auth.outputs.token }}
        run: |
          set -e

          # Get expected version from Cargo.toml
          EXPECTED_VERSION=$(grep -m1 '^version' Cargo.toml | cut -d'"' -f2)
          echo "Expected version: $EXPECTED_VERSION"

          # Check if a specific version is already published via crates.io API
          is_published() {
            local crate=$1
            local version=$2
            curl -sf "https://crates.io/api/v1/crates/$crate" 2>/dev/null | \
              jq -e ".versions[] | select(.num == \"$version\")" > /dev/null 2>&1
          }

          # Publish a crate (oxc-style: API check before publish)
          publish_crate() {
            local crate=$1
            echo "Publishing $crate v$EXPECTED_VERSION..."

            # Check if already published via API
            if is_published "$crate" "$EXPECTED_VERSION"; then
              echo "$crate v$EXPECTED_VERSION is already published, skipping."
              return 0
            fi

            # Publish
            if ! cargo publish -p "$crate"; then
              echo "Failed to publish $crate"
              return 1
            fi

            echo "$crate v$EXPECTED_VERSION published successfully."
            return 0
          }

          # Publish in dependency order
          # With sparse registry, index updates are nearly instant after publish
          publish_crate vize_carton
          publish_crate vize_relief
          publish_crate vize_armature
          publish_crate vize_croquis
          publish_crate vize_atelier_core
          publish_crate vize_atelier_dom
          publish_crate vize_atelier_vapor
          publish_crate vize_atelier_sfc
          publish_crate vize_atelier_ssr
          # vize_glyph, vize_vitrine, vize_maestro, vize are skipped:
          # vize_glyph depends on oxc_formatter which is not yet on crates.io,
          # and the others depend on vize_glyph (even as optional, crates.io still resolves it)
          publish_crate vize_patina
          publish_crate vize_canon
          publish_crate vize_musea
          publish_crate vize_fresco

  # Create GitHub Release with CLI binaries
  create-github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [build-cli, release-npm-native, release-npm-fresco-native, release-npm-fresco, release-npm-wasm, release-npm-vite-plugin, release-npm-unplugin, release-npm-rspack-plugin, release-npm-musea-mcp-server, release-npm-vite-plugin-musea, release-npm-musea-nuxt, release-npm-nuxt, release-crates]
    steps:
      - uses: actions/checkout@v4

      - name: Download CLI artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: cli-*
          path: cli-artifacts
          merge-multiple: true

      - name: List CLI artifacts
        run: ls -la cli-artifacts/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
          files: |
            cli-artifacts/*.tar.gz
            cli-artifacts/*.zip

  # Publish vize CLI npm package (using Trusted Publishing / OIDC)
  # Native bindings are distributed via @vizejs/native-* optionalDependencies (NAPI-RS)
  release-npm-cli:
    name: Release vize CLI to npm
    runs-on: ubuntu-latest
    needs: [release-npm-native, create-github-release]
    environment: npm
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - name: Install npm with OIDC support
        run: npm install -g npm@latest

      - name: Inject native binding package versions
        run: |
          node -e "
            const fs = require('fs');
            const pkg = JSON.parse(fs.readFileSync('npm/vize/package.json', 'utf8'));
            const version = pkg.version;
            if (pkg.optionalDependencies) {
              for (const dep of Object.keys(pkg.optionalDependencies)) {
                if (dep.startsWith('@vizejs/native-')) {
                  pkg.optionalDependencies[dep] = version;
                }
              }
            }
            fs.writeFileSync('npm/vize/package.json', JSON.stringify(pkg, null, 2) + '\n');
            console.log('Updated optionalDependencies to ' + version);
          "
          cat npm/vize/package.json

      - name: Build vize CLI package
        working-directory: npm/vize
        run: pnpm install --no-frozen-lockfile && pnpm build

      - name: Publish vize CLI package
        working-directory: npm/vize
        run: pnpm publish --provenance --access public --no-git-checks --tag $NPM_TAG
